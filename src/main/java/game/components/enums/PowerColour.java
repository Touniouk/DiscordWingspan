package game.components.enums;

/**
 * Colours indicating when a bird's power activates:
 * WHITE = when played, BROWN = when activated, PINK = other players' turns,
 * TEAL = end of round, YELLOW = end of game.
 */
public enum PowerColour {
    WHITE,
    BROWN,
    PINK,
    TEAL,
    YELLOW
}
